# Only needed for the command-line scripts (npm run one, npm run eval, scripts/record-demos.ts).
# The website never uses these: visitors paste their own key in the page.
OPENROUTER_API_KEY=
# Optional alternative for the scripts (TypeSafe blocks browser calls, so the site cannot use it).
TYPESAFE_API_KEY=
